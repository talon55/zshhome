# ----------------------------------------
#   WSL-Specific configuration for ZSH
# ----------------------------------------

# Set WSL default browser
export BROWSER=wslview

# Use default docker context in WSL
export DOCKER_CONTEXT=default
